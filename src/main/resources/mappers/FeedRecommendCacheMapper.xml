<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ht.project.snsproject.mapper.FeedRecommendCacheMapper">
    <select id="getFeedInfoByLatestOrder" resultType="com.ht.project.snsproject.model.feed.FeedInfoCache">
        SELECT a.id, a.userId,
        a.title, a.content,
        a.date, a.publicScope,
        b.filePath, GROUP_CONCAT(b.fileName ORDER BY b.fileIndex ASC) AS fileNames
        FROM feeds a
        LEFT JOIN images b
        ON (a.id = b.feedId)
        WHERE a.publicScope = 'ALL'
        GROUP BY a.id
        ORDER BY a.id DESC
        limit 10;
    </select>
</mapper>